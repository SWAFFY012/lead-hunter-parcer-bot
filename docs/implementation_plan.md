# LeadHunter — План реализации (Обновленный)

## 📌 Что уже выполнено (Completed)

### ✅ Фаза 1 — Скаффолдинг проекта и Архитектура
- Развернута monorepo: `/backend` (Express, better-sqlite3, Socket.io) + `/frontend` (Vite, React, TypeScript).
- Создана и настроена база данных `leads.db`.
- Настроены базовые роуты и UI-компоненты.

### ✅ Фаза 3 — Безопасный Парсинг OLX (Anti-Ban)
*Вместо Playwright мы внедрили более безопасный подход через Chrome Extension.*
- **Manual Parser**: Лиды собираются прямо из браузера пользователя через расширение (обходим любые капчи).
- **Smart Deduplication**: Умное слияние дубликатов (номера телефонов). Если номер уже есть, скрипт не падает, а дописывает новые объявления к существующему лиду. Поддержка мульти-аккаунтов OLX.
- **UI**: Раздел "Ручные базы" с таблицей лидов, сортировкой, статусами и красивым форматированием ссылок.

### ✅ Фаза 4 — AI-Генерация и Ревью
- **Очередь и таймауты**: Последовательная `for...of` генерация через локальную Ollama. Защита от зависаний (120с таймаут и graceful обработка ошибок).
- **Spintax & Variables**: Поддержка синтаксиса `{A|B}` и переменных `{{name}}`, `{{title}}`.
- **System Skills (Global Rules)**: Единые коммерческие правила для всех промптов хранятся в БД `global_settings` и склеиваются с контекстом перед отправкой в LLM.
- **UI Ревью**: Массовое выделение (чекбоксы), привязка промпта к базе, редактируемые textarea, модальное окно для детального редактирования.
- **Резервные каналы**: Fallback-кнопки (🟢 WhatsApp, 🟣 Viber, 🔵 Telegram) для ручной отправки сообщений прямо из CRM.

---

## 🚀 Предложения по улучшению (Ideas for the future)

Прежде чем переходить к финальным стадиям, вот несколько идей, как мы можем сделать приложение еще мощнее:
1. **Облачный AI (Fallback)**: Подключение API ключа OpenAI / Anthropic / DeepSeek в настройках. Если локальная Ollama не тянет или недоступна, генерация незаметно переключается на облако.
2. **Аналитика (Dashboard)**: Графики конверсий. Сколько отправлено, сколько ответили (в %). Тепловая карта рассылок по дням недели.
3. **Smart Replies**: Когда лид отвечает в WhatsApp, AI может автоматически проанализировать его ответ (позитив/негатив) и предложить вам вариант ответа.

---

## 📋 План на следующие фазы (К выполнению)

### 📱 Фаза 2 — WA Аккаунты (Baileys)
**Цель:** Подключение WhatsApp аккаунтов через QR и управление сессиями.
1. Настроить библиотеку `@whiskeysockets/baileys` в бэкенде.
2. Создать эндпоинты для генерации QR-кода и получения статуса (online, offline).
3. Разработать UI в разделе `Accounts` для добавления аккаунтов (сканирование QR-кода прямо на экране).
4. Настроить механизм переподключения (сохранение auth state в папку `sessions/`).

### 📨 Фаза 5 — Модуль Рассылки (Sender)
**Цель:** Автоматическая отправка утвержденных (`ready_to_send`) AI-сообщений.
1. Бэкенд-модуль рассылки, который берет лиды со статусом `ready_to_send`.
2. Ротация аккаунтов (распределение отправки поровну между всеми онлайн WA-аккаунтами).
3. Имитация человека: отправка статуса `typing...` на 3-8 секунд перед сообщением + рандомные задержки между сообщениями (например, от 2 до 10 минут), чтобы избежать бана.
4. UI: Раздел `Sender` с кнопками "Старт/Пауза" и живым логом рассылки через Socket.io.
5. Автоматическая смена статуса лида на `sent` после успешной отправки.

### 🤖 Фаза 6 — Обратная связь и Дашборд
**Цель:** Завершение CRM.
1. Слушатель входящих сообщений (Webhooks/Baileys events): если лид со статусом `sent` что-то пишет, статус меняется на `replied`.
2. Тост-уведомление в браузере при новом ответе.
3. Разработка главной страницы `Dashboard` с выгрузкой метрик из SQLite.


# Фаза 7: Оптимизация продаж и удержание лидов (Pipeline & Retention)

На основании вашего запроса, мы откладываем мульти-парсинг и фокусируемся на трех мощнейших механиках для "дожима" лидов и удобства менеджера. 

## 1. 🗂 Kanban-доска (Визуальная воронка)
Табличный вид идеален для массовой рассылки, но для ведения теплых диалогов нужна наглядность.

**Что сделаем:**
- Добавим новую вкладку в боковое меню: `Воронка` (Pipeline).
- Используем легковесную библиотеку (например, `@hello-pangea/dnd`) для Drag-and-Drop интерфейса.
- Создадим колонки под ключевые этапы: `Ответили` ➡️ `В работе (Переговоры)` ➡️ `Сделка` ➡️ `Отказ`.
- При перетягивании карточки лида из колонки в колонку, фронтенд будет автоматически отправлять `PATCH /api/leads/:id` для смены статуса.
- В карточке на доске будет отображаться Имя, Последнее сообщение и таймер (сколько времени лид висит без ответа).

## 2. 🤖 AI-Суфлер в чате (Smart Replies)
Повышаем скорость ответов менеджера, используя уже настроенную локальную модель Qwen 2.5.

**Что сделаем:**
- В интерфейсе чата (карточка лида) рядом с полем ввода добавим кнопку `✨ Сгенерировать ответ`.
- Бэкенд-эндпоинт: `POST /api/leads/:id/generate_reply`.
- Логика: Бэкенд берет последние 4-5 сообщений из переписки, формирует системный промпт (например, "Действуй как вежливый менеджер по продажам, ответь на вопрос клиента коротко и по делу") и отправляет в Ollama.
- Ollama генерирует текст (с `keep_alive: 0` для экономии памяти) и возвращает его.
- Текст подставляется в поле ввода (не отправляется сразу!). Менеджер может его отредактировать и нажать "Отправить".

## 3. ⏰ Система Follow-up (Авто-дожимы)
Не даем лидам остыть, если они проигнорировали первое сообщение.

**Что сделаем:**
- Добавим в таблицу `leads` поле `followup_count` (по умолчанию 0).
- Напишем фоновый Cron-скрипт (или `setInterval` на бэкенде), который раз в час проверяет базу.
- Если лид находится в статусе `sent` (то есть не ответил) более 24 часов, и `followup_count == 0`, система автоматически отправляет мягкое сообщение (например: *"Здравствуйте! Удалось ли посмотреть мое предыдущее сообщение?"*).
- Поле `followup_count` увеличивается до 1. Если ответа нет еще 48 часов, можно отправить финальный дожим.
- Добавим UI-настройку на Дашборд или в Настройки для включения/выключения авто-дожимов.

---

## User Review Required
> [!IMPORTANT]
> Эти 3 фичи превратят вашу систему из "просто рассыльщика" в полноценный инструмент закрытия сделок.
> 
> Какой из этих пунктов вы хотите реализовать **в первую очередь**? Жмите **Proceed**, если утверждаете план, и укажите в чате с чего мы начнем!
